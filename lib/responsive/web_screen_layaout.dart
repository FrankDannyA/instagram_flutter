import 'package:flutter/material.dart';

class WebScreenLayaout extends StatelessWidget {
  const WebScreenLayaout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('This is web screen')),
    );
  }
}
